name: nested_home_page
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  go_router: 17.5.0
  meta: 1.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  go_router_builder: 4.4.0
  build_runner: ^2.15.1

flutter:
  uses-material-design: true
